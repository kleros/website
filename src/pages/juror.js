import React from "react";

if (typeof window !== `undefined` && window.location.pathname.match(/\/\w{2}\/juror\/*/)) window.location.replace('https://blog.kleros.io/become-a-juror-blockchain-dispute-resolution-on-ethereum/'); // highlight-line

const Juror = () => <></>;

export default Juror;
