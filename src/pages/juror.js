import React from "react";

if (typeof window !== `undefined` && window.location.pathname.match(/\/en\/juror\/*/)) window.location.replace('https://blog.kleros.io/become-a-juror-blockchain-dispute-resolution-on-ethereum/'); // highlight-line

const Juror = () => <></>;

export default Juror;
